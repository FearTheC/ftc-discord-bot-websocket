version: '3.3'

services:
  client:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "7632"
    tty: true
    restart: always
    env_file:
      - .env
    volumes:
      - .:/app:z
    links: 
      - broker
      - cache
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      ftcbot:
    
  broker:
    image: rabbitmq:3.7-alpine
    expose:
      - "4369"
      - "5671"
      - "5672"
      - "25672"
    networks:
      ftcbot:
      
  cache:
    image: redis:alpine
    expose:
      - "6379"
    volumes:
      - redis_data:/data:z
    networks:
      ftcbot:
      
networks:
    ftcbot:
 
volumes:
  redis_data:
 